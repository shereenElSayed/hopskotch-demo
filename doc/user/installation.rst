============
Installation
============

.. contents::
   :local:

You can install hop-demo-app via pip or from source.

To install with pip:

.. code:: bash

   pip install -U hop-demo-app

To install from source:

.. code:: bash

    tar -xzf hop-demo-app-x.y.z.tar.gz
    cd hop-demo-app-x.y.z
    python setup.py install
